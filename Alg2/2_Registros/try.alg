algoritmo "changeToReferencia"
var |
procedimento changeRef(var nomeVar:tipoVar,...)
    var | temp : inteiro
    inicio | temp <- x| x <- y | y <- temp //rule
fimprocedimento
inicio
changeRef(nomeVar1,nomeVar2)
fimalgoritmo